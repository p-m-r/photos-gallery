import { GalleryDirectiveDirective } from './gallery-directive.directive';

describe('GalleryDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GalleryDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
