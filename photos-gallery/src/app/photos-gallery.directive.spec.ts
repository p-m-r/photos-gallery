import { PhotosGalleryDirective } from './photos-gallery.directive';

describe('PhotosGalleryDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotosGalleryDirective();
    expect(directive).toBeTruthy();
  });
});
